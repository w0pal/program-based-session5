// add function to sum number
const addNumber = (number1,number2) => {
    return number1 + number2
}

const subNumber = (number1,number2) => {
    return number1 - number2
}

const multiplyNumber = (number1,number2) => {
    return number1 * number2
}

module.exports = {addNumber,subNumber,multiplyNumber}